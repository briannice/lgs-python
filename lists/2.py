# In the second task we only have one list of integers. The task is to
# concatenate the numbers and end up with a single integer as a result. It's
# useful to start with the string type, so the += operation doesn't add the
# next element to the sum but simply inserts it to the end.

numbers = [12, 54, 812]